{
  "name": "jopi-loader",
  "version": "1.0.10",
  "private": false,
  "license": "MIT",
  "keywords": ["loader"],
  "description": "Node ESM loader to take in charge non-JS assets (.scss/.sass/.css, images, fonts)",

  "engines": {
    "bun": "^1.2.18",
    "node": ">=22.18.0"
  },

  "main": "./dist/index.js",
  "type": "module",

  "bin": {
    "jopin": "bin/jopin.js",
    "jopib": "bin/jopib.js"
  },

  "scripts": {
    "tsc": "npx tsc",
    "bun-publish": "bun publish"
  },

  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@jopi-loader/tools": "workspace: *",
    "@jopi-loader/types": "workspace: *"
  }
}